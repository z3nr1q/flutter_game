import 'package:flame/game.dart';
import 'package:flutter/material.dart';

class PuzzleGame extends FlameGame {
  @override
  Future<void> onLoad() async {
    await super.onLoad();
    // TODO: Carregar recursos iniciais do jogo
  }

  @override
  void update(double dt) {
    super.update(dt);
    // TODO: Atualizar lógica do jogo
  }

  @override
  void render(Canvas canvas) {
    super.render(canvas);
    // TODO: Renderizar elementos do jogo
  }
}
